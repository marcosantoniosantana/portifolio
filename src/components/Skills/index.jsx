import { DiReact } from "react-icons/di";
import "./style.scss"

export default function Skills(){
    return(
        <section id="skills" className="skills">
            <DiReact className="cor"/>
        </section>
    )
}